$('#comment-form-row').remove();

<% if @post.comments.empty? %>
  $(".comment-section").after("<%= j render('form') %>");
<% else %>
  $(".comments-wrapper").append("<%= j render('form') %>");
<% end %>

$('#new_comment').prepend('<h5>New Comment</h5>');
$('#comment_text').characterCounter();
